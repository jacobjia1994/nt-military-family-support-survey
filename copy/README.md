# Adult reading copy

Generated from the current `survey.js` definitions using `node scripts/export-copy.mjs`. Formal participant wording remains separate from this staff documentation.

The 25 September revision has 16 adult need domains plus an open alternative, 12-month adult/youth recall, a separate earlier-NT-experience route and one follow-up page per selected current need. Schema 3 stores impact/help/barriers/change in `follow_up[need_id]`; general delivery preferences remain separate. The reading view shows the repeatable per-need template and the two conditional barrier variants, rather than duplicating every page for all 16 needs.

See [strategy source mapping](strategy-needs-map.md), [LC policy and form evidence](lc-public-practice.md), and the [staff guide](../review.html). Adult/youth comment fields allow 5000 characters, children's fields 1500; those limits are capacity ceilings, not requested response lengths.
